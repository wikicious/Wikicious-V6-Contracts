{
  "name": "wikicious-v6-contracts",
  "version": "6.0.0",
  "description": "Wikicious V6 — Smart Contracts on Arbitrum One",
  "scripts": {
    "compile":        "hardhat compile",
    "test":           "hardhat test",
    "deploy":         "hardhat run scripts/deploy.js --network arbitrum_one",
    "deploy:testnet": "hardhat run scripts/deploy.js --network arbitrum_sepolia",
    "verify":         "hardhat run scripts/verify.js --network arbitrum_one",
    "transfer":       "hardhat run scripts/transfer-ownership.js --network arbitrum_one"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.15",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "@chainlink/contracts": "^1.3.0"
  }
}
